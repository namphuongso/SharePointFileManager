export interface Messages {
  files: string;
  name: string;
  modified: string;
  size: string;
  empty: string;
  emptyHint: string;
  retry: string;
  refresh: string;
  loadMore: string;
  unknownError: string;
}
